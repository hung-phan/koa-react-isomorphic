type BlogSchema {
  node(id: ID!): Node
  todos: [Todo]
}

interface Node {
  id: ID!
}

type Todo implements Node {
  id: ID!
  user: User
  text: String
  complete: Boolean
}

type User implements Node {
  id: ID!
  name: String
}
